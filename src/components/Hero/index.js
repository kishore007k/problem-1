import React from "react";
import UploadForm from "../UploadForm";
import ImageEditor from "../ImageEditor";

const Hero = () => {
	return (
		<div>
			<ImageEditor />
			<UploadForm />
		</div>
	);
};

export default Hero;
